package com.example;

import android.content.Context;

import com.facebook.react.ReactInstanceManager;

/**
 * Dummy Flipper initialization. This allows {@link io.radar.example.MainApplication} to call the
 * {@link #initializeFlipper(Context, ReactInstanceManager)} method without reflection. The debug variant of this
 * application uses this same method signature to initialize Flipper.
 */
public class ReactNativeFlipper {
  public static void initializeFlipper(Context context, ReactInstanceManager reactInstanceManager) {
    // Do nothing
  }
}